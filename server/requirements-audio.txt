# Синтез аудиообзоров. Отдельным файлом, а не в requirements.txt, потому что API
# аудио не синтезирует — он раздаёт готовые файлы. Синтез нужен только на сборке.
#
#   pip install -r requirements-audio.txt
#   python -m scripts.generate_audio_scripts   # диалоги (нужен ключ провайдера)
#   python -m scripts.build_audio              # озвучка
#
# edge-tts обращается к тому же сервису синтеза, что и чтение вслух в браузере Edge.
# Ключа он не требует, но это и не документированный API: доступ могут закрыть.
# Риск ограничен сборкой — готовые mp3 лежат у нас и раздаются локально.
edge-tts==7.2.8
lameenc==1.8.4
